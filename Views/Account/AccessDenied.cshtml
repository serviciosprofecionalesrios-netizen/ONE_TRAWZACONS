@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="container mt-5" style="max-width: 560px;">
    <div class="card shadow-sm">
        <div class="card-body text-center">
            <h3 class="text-danger mb-3">Acceso denegado</h3>
            <p class="text-muted mb-4">No tienes permisos para acceder a este recurso.</p>
            <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary">Ir al inicio</a>
        </div>
    </div>
</div>
